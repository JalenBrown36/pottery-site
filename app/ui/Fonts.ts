import { Urbanist, Grand_Hotel } from "next/font/google";

export const urbanist = Urbanist({ 
    subsets: ["latin"] });
export const grandHotel = Grand_Hotel({ 
    subsets: ["latin"],
    weight: ['400'],
});